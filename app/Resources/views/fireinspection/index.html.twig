{% extends 'base.html.twig' %}

    {% block title %}Lista przeglądów{% endblock %}

    {% block body %}
        <!-- Content header -->
        <div class="content-header row">
            <div class="content-header-left col-md-6 col-12">
                <h3 class="content-header-title mb-0">Baza przeglądów</h3>
                <div class="row breadcrumbs-top">
                    <div class="breadcrumb-wrapper col-12">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="{{ path('micro_default_index') }}">Główna</a>
                            </li>

                            <li class="breadcrumb-item active">Lista przeglądów PPOŻ
                            </li>

                        </ol>
                    </div>
                </div>
            </div>
            <div class="content-header-left col-md-6 col-12">
                <div class="btn-group float-md-right" role="group" aria-label="Button group with nested dropdown">
                    <div class="btn-group" role="group">
                        <a class="btn btn-outline-info round" href="{{ path('fireinspection_index') }}"><i
                                    class="la la-user-plus"></i> Dodaj nowy</a>

                        <button class="btn btn-outline-info round dropdown-toggle dropdown-menu-right"
                                id="btnGroupDrop1"
                                type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i
                                    class="icon-cog3 icon-left"></i> Ustawienia
                        </button>
                        <div class="dropdown-menu" aria-labelledby="btnGroupDrop1"><a class="dropdown-item"
                                                                                      href="card-bootstrap.html">Bootstrap
                                Cards</a><a class="dropdown-item" href="component-buttons-extended.html">Buttons
                                Extended</a></div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Content header -->


        <section class="row all-contacts">
            <div class="col-12">
                <div class="card">
                    <div class="card-head">
                        <div class="card-header">
                            <h4 class="card-title">Lista przeglądów PPOŻ</h4>
                            <div class="heading-elements mt-0">
                            </div>
                        </div>
                    </div>
                    <div class="card-content">
                        <div class="card-body">
                            <!-- Task List table -->
                            {#<button type="button" class="btn btn-sm btn-danger delete-all mb-1">Delete All</button>#}
                            <div class="table-responsive">
                                <table id="users-contacts"
                                       class="table table-white-space table-bordered row-grouping display no-wrap icheck table-middle text-center">
                                    <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Deviceshortlistposition</th>
                                        <th>Inspectiondate</th>
                                        <th>Nextinspectiondate</th>
                                        <th>Comment</th>
                                        <th>Otheractivities</th>
                                        <th>Actions</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for fireInspection in fireInspections %}
                                        <tr>
                                            <td>
                                                <a href="{{ path('fireinspection_show', { 'id': fireInspection.id }) }}">{{ fireInspection.id }}</a>
                                            </td>
                                            <td>{{ fireInspection.deviceShortlistPosition }}</td>
                                            <td>{% if fireInspection.inspectionDate %}{{ fireInspection.inspectionDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                                            <td>{% if fireInspection.nextInspectionDate %}{{ fireInspection.nextInspectionDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                                            <td>{{ fireInspection.comment }}</td>
                                            <td>{{ fireInspection.otherActivities }}</td>


                                            <td>
                                                <a href="{{ path('fireinspection_edit', { 'id': fireInspection.id }) }}"
                                                   class="primary edit mr-1"><i class="la la-pencil"></i></a>
                                                <a class="danger delete mr-1"><i class="la la-trash-o"></i></a>

                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                    <tfoot>
                                    <tr>
                                        <th>Id</th>
                                        <th>Deviceshortlistposition</th>
                                        <th>Inspectiondate</th>
                                        <th>Nextinspectiondate</th>
                                        <th>Comment</th>
                                        <th>Otheractivities</th>
                                        <th>Actions</th>
                                    </tr>
                                    </tfoot>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

    {% endblock %}

    {% block scripts %}
        <script src="{{ asset('app-assets/vendors/js/tables/datatable/datatables.min.js') }}"></script>

        <script src="{{ asset('app-assets/js/scripts/tables/datatables/datatable-basic.js') }}"></script>

    {% endblock %}
